---
import Base from './Base.astro';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  updatedDate?: Date;
  tags?: string[];
}

const { title, description, pubDate, updatedDate, tags = [] } = Astro.props;

const formattedPubDate = pubDate.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});

const formattedUpdatedDate = updatedDate?.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<Base title={title} description={description}>
  <article class="max-w-3xl mx-auto px-4 sm:px-6 py-12">
    <header class="mb-8">
      <h1 class="text-2xl sm:text-3xl font-bold text-midnight-950 dark:text-midnight-100 mb-4">
        {title}
      </h1>
      <div class="flex flex-wrap items-center gap-4 text-sm text-midnight-400">
        <time datetime={pubDate.toISOString()}>
          {formattedPubDate}
        </time>
        {updatedDate && (
          <span>
            (Updated: <time datetime={updatedDate.toISOString()}>{formattedUpdatedDate}</time>)
          </span>
        )}
      </div>
      {tags.length > 0 && (
        <div class="flex flex-wrap gap-2 mt-4">
          {tags.map((tag) => (
            <span class="px-2 py-1 text-xs rounded-md bg-midnight-800 text-midnight-300">
              {tag}
            </span>
          ))}
        </div>
      )}
    </header>

    <div class="prose prose-invert prose-lg max-w-none
      prose-headings:text-midnight-100
      prose-h1:text-xl prose-h1:sm:text-2xl
      prose-h2:text-lg prose-h2:sm:text-xl
      prose-p:text-midnight-200
      prose-a:text-truth-400 prose-a:no-underline hover:prose-a:underline
      prose-strong:text-midnight-100
      prose-code:text-truth-300 prose-code:bg-midnight-800 prose-code:px-1 prose-code:rounded
      prose-pre:bg-midnight-900 prose-pre:border prose-pre:border-midnight-700
      prose-blockquote:border-truth-500 prose-blockquote:text-midnight-300 prose-blockquote:font-serif prose-blockquote:italic
      prose-li:text-midnight-200
    ">
      <slot />
    </div>
  </article>
</Base>

<style>
  :global(html:not(.dark)) article time,
  :global(html:not(.dark)) article .text-midnight-400 {
    @apply text-midnight-950;
  }
  :global(html:not(.dark)) article .bg-midnight-800 {
    @apply bg-midnight-200 text-midnight-950;
  }
  :global(html:not(.dark)) .prose {
    @apply prose-gray;
  }
  :global(html:not(.dark)) .prose p,
  :global(html:not(.dark)) .prose li,
  :global(html:not(.dark)) .prose h1,
  :global(html:not(.dark)) .prose h2,
  :global(html:not(.dark)) .prose h3,
  :global(html:not(.dark)) .prose h4,
  :global(html:not(.dark)) .prose strong,
  :global(html:not(.dark)) .prose blockquote {
    @apply text-midnight-950;
  }
  :global(html:not(.dark)) .prose a {
    @apply text-truth-700;
  }
  :global(html:not(.dark)) .prose code {
    @apply bg-midnight-100 text-truth-800;
  }
</style>
